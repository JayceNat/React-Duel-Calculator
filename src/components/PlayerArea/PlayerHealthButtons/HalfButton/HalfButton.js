import React from 'react';
import classes from './HalfButton.module.css';

const halfButton = (props) => {
    return (
        <div className={classes.halfButton}>1/2</div>
    );
};

export default halfButton;
import React from 'react';
import classes from './PlayerHealthPoint.module.css';

const playerHealthPoint = (props) => {
    return (
        <div className={classes.PlayerHealthPoint}>8000</div>
    );
};

export default playerHealthPoint;